server:
  port: 9200

spring:
  application:
    name: yulao
  datasource:
    url: jdbc:mysql://localhost:3306/yulao?useSSL=false&serverTimezone=UTC
    username: admin
    password: StrongPassword123!
    driver-class-name: com.mysql.cj.jdbc.Driver


# MyBatis-Plus 日志（可选）
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

